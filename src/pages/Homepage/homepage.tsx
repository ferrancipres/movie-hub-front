import { MovieContent } from "../../components"

export const Homepage = () => {
    return (
        <main>
            <section>
                <MovieContent />
            </section>
        </main>
    )
};